# Environment setup
.envrc

# Python / uv
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Local artifacts (corpus cache, prepared dataset, runs)
/data/
outputs/
wandb/

# Caches the training stack writes into the working tree on Colab (they mark runs git_dirty)
huggingface_tokenizers_cache/
unsloth_compiled_cache/
